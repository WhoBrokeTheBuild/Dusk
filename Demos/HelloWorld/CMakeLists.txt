
SET(_TARGET HelloWorld)

ADD_SHADER_DATA_BINDING(ASDF)
ADD_SHADER_INPUT_LOCATION(ASDF)

DUSK_DEMO(${_TARGET})

TARGET_LINK_LIBRARIES(
    ${_TARGET}
    DuskOpenGL
)

ADD_DEPENDENCIES(
    ${_TARGET} 
    DuskSTBI 
    DuskOpenGL
)
